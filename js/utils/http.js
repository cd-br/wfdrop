const TIMEOUT_PADRAO_MS=8e3;export async function fetchComTimeout(t,{timeoutMs:e=8e3,signal:o}={}){const r=new AbortController;o&&(o.aborted?r.abort():o.addEventListener("abort",()=>r.abort()));const a=setTimeout(()=>r.abort(),e);try{return await fetch(t,{signal:r.signal})}finally{clearTimeout(a)}}export class ErroTimeout extends Error{constructor(){super("A requisição demorou demais e foi cancelada."),this.name="ErroTimeout"}}